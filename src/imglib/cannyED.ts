import {ImageFactory, Image2D, ImagePixels, ImageUint8} from './imagebase'
import Gradient from './gradient'

export default class CannyEdgeDetection {

    /*
    Detect(inImage:Image2D, lowThr : number, highThr : number ) : ImageUint8 {
        let nsImage = NonMaxSuppressedImage(inImage)
    }


    private NonMaxSuppressedImage ( inImage : Image2D ) : ImageUint16 {

    }
    */

}